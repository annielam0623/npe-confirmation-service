# 版本控制
.git
.gitignore

# Python 缓存与虚拟环境
__pycache__
*.pyc
*.pyo
*.pyd
.Python
venv
.venv
env/
.venv/

# 本地密钥 / 环境变量（绝不能进镜像，容器靠 ECS 注入环境变量）
.env
.env.*

# 平台配置（ECS 不用 Railway 的配置）
railway.toml

# IDE / 编辑器
.vscode
.idea
*.swp
.DS_Store

# 测试 / 临时产物
.pytest_cache
.coverage
htmlcov

# 说明：保守版 —— 只排真正不该进镜像的。
# 模板(.html)、静态资源(.png/.css/.js)、数据文件(.json)、SQL、文档全部打进镜像，
# 确保第一次 build 不漏任何运行时要读的文件。跑通后再考虑精简体积。
